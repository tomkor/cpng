name: minio
defaultNamespace: minio
labels:
  app: minio
dependsOn:
  - selector:
      matchLabels:
        app: minio-secret
helm:
  chart: oci://registry-1.docker.io/bitnamicharts/minio
  version: 17.0.21
  valuesFiles:
    - helm-values.yaml